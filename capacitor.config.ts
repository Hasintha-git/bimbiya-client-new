import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'bimbiya-client',
  webDir: 'dist/bimbiya-production'
};

export default config;
